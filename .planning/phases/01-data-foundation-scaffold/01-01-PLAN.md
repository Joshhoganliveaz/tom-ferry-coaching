---
phase: 01-data-foundation-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [projects.json]
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "projects.json contains a valid JSON array of ~16 projects with all required fields"
    - "Each project has name, description, status, health scores using enum values (not numeric)"
    - "Shipped projects have deployedUrl and deployedDate; parking_lot projects have priority scores"
    - "Version history arrays are ordered newest-first with date and notes"
  artifacts:
    - path: "projects.json"
      provides: "Complete project data for all ~16 Live AZ Co projects"
      contains: "lastUpdated"
  key_links:
    - from: "projects.json"
      to: "index.html (Plan 02)"
      via: "fetch() call with cache-busting"
      pattern: "fetch.*projects\\.json"
---

<objective>
Create the projects.json data file with complete schema and seed data for all ~16 Live AZ Co projects.

Purpose: This is the single source of truth that the entire tracker renders from. Every card, health bar, filter, and stat in later phases depends on this data being accurate and well-structured.

Output: `projects.json` at repo root with ~16 project entries, each containing name, description, status, health scores (enum-based), version history, upNext, and priority scores where applicable.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation-scaffold/01-RESEARCH.md
@docs/superpowers/specs/2026-03-25-project-tracker-design.md

Key data sources for seed data:
- Design spec "Seed Data" section lists all ~16 projects by status category
- CLAUDE.md (user's home directory) has details for STR Analyzer, LiveAZCo-CMA, Idea Pipeline, Tenet Calculator
- Project memory files in ~/.claude/projects/ may have additional context

<interfaces>
<!-- JSON schema contract from the design spec (authoritative source) -->

Status enum: "shipped" | "in_progress" | "parking_lot"
Adoption enum: "none" | "josh_only" | "team_uses" | "client_facing"
Autonomy enum: "josh_dependent" | "needs_guidance" | "fully_independent"
Revenue proximity enum: "internal_only" | "client_touch" | "revenue_driver"

Priority object (unshipped only): { makesMoney: 1-5, savesTime: 1-5, servesClients: 1-5, speedToFinish: 1-5 }
UpNext object (optional): { version?: string, notes: string }
Version entry: { version: string, date: string, notes: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create projects.json schema and seed data</name>
  <files>projects.json</files>
  <action>
Create `projects.json` at the repo root with the following structure:

Top-level object:
- `lastUpdated`: "2026-03-25"
- `projects`: array of project objects

Populate ALL projects from the design spec seed data list. Use the enum-based health scoring system from the design spec (NOT numeric 1-10). For each project:

**Shipped (5 projects):**
1. Contract Summary Template -- v1.0, team_uses, fully_independent, client_touch
2. CMA Report Generator / Real Estate Review -- reference CLAUDE.md for stack (Flask, Python), deployed at realestatereview.onrender.com. josh_only, josh_dependent, client_touch
3. STR Analyzer -- reference CLAUDE.md for stack (Next.js, TypeScript, Supabase), deployed at stranalyzer.vercel.app. client_facing, needs_guidance, client_touch. Include version history from CLAUDE.md (v2.0 Supabase integration, v2.1 public investor view)
4. Tenet Commission Calculator -- v1.0, josh_only, fully_independent, internal_only. No deploy URL (local HTML file)
5. Idea Pipeline -- reference CLAUDE.md for stack (Next.js, Prisma, Claude API). josh_only, josh_dependent, internal_only

**In Progress (6 projects):**
6. Houseversary Batch Dashboards
7. Transaction Checklist Auto-Builder
8. Appraisal Brief Self-Serve Tool
9. Daily Schedule Dashboard
10. Dashboard Generator Platform
11. Direct Booking Re-Engagement Dashboard

For in_progress projects: set reasonable health scores based on what is known. Include priority scores object with makesMoney, savesTime, servesClients, speedToFinish as 1-5 ranks. Use upNext where applicable.

**Parking Lot (5 projects):**
12. Conversational CRM
13. iMessage Automation
14. Communication Bot (53k texts)
15. Suzie's Role Automation (TC to Partner Agent)
16. YouTube / B-Roll Pipeline

For parking_lot projects: adoption=none, autonomy=josh_dependent, revenueProximity=internal_only (or appropriate). Include priority scores. No versions array (empty array), no deployedUrl, no deployedDate.

Rules:
- Use underscore_separated enum values per spec: "in_progress" not "in-progress"
- versions arrays ordered newest-first
- priority is null for shipped projects
- upNext is null when no roadmap data exists
- deployedUrl and deployedDate are null when not deployed
- Add a JSON comment-style approach: where data is uncertain, set the value to a reasonable estimate (do NOT use TODO strings in values -- use real plausible data that can be refined later)
- Ensure valid JSON (no trailing commas, no comments)
  </action>
  <verify>
    <automated>cd "/Users/joshuahogan/Projects/Tom Ferry Coaching" && python3 -c "import json; d=json.load(open('projects.json')); print(f'{len(d[\"projects\"])} projects loaded'); assert len(d['projects']) >= 15, 'Need at least 15 projects'; assert all(p['status'] in ['shipped','in_progress','parking_lot'] for p in d['projects']), 'Invalid status enum'; print('Schema OK')"</automated>
  </verify>
  <done>projects.json exists at repo root with 16 project entries, all using enum-based health scores, valid status values, and appropriate version histories. Python validation script confirms schema correctness.</done>
</task>

</tasks>

<verification>
- `python3 -c "import json; d=json.load(open('projects.json')); print(len(d['projects']), 'projects')"` outputs 16
- Every project has name, description, status, adoption, autonomy, revenueProximity fields
- Shipped projects have priority=null; unshipped have priority object with 4 numeric fields
- No "undefined", "TBD", or placeholder strings in any field values
</verification>

<success_criteria>
- projects.json is valid JSON with ~16 project entries
- All enum values match the design spec (shipped/in_progress/parking_lot, none/josh_only/team_uses/client_facing, etc.)
- Shipped projects have version histories; parking lot projects have empty version arrays
- File is ready to be consumed by index.html fetch pipeline in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation-scaffold/01-01-SUMMARY.md`
</output>
