---
phase: 02-card-rendering-interactivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [CARD-01, CARD-02, CARD-03, CARD-04, CARD-05, NAV-01]

must_haves:
  truths:
    - "Each project displays as a card with name, color-coded status badge, version, deployed date, and clickable deploy URL (or 'Not deployed' for null URLs)"
    - "Three health bars per card show Adoption/Autonomy/Revenue Proximity with color-coded fills (canyon/gold/olive) and human-readable text labels"
    - "Summary stats bar at top shows live counts: Shipped, In Progress, Parking Lot, Fully Independent, Josh Dependent, Client Facing"
    - "Version history expands per card via details/summary showing entries newest-first with date and notes"
    - "Up Next box with gold highlight appears only on cards where upNext data exists"
    - "Priority scores (makesMoney, savesTime, servesClients, speedToFinish as X/5) appear only on in_progress and parking_lot cards"
  artifacts:
    - path: "index.html"
      provides: "Card rendering with health bars, version history, conditional sections, summary stats"
      contains: "renderCard"
  key_links:
    - from: "renderPage()"
      to: "window.__projects"
      via: "iterates projects array to build card grid"
      pattern: "renderCard"
    - from: "renderSummaryStats()"
      to: "window.__projects"
      via: "computes counts from projects array"
      pattern: "computeStats"
    - from: "renderHealthBars()"
      to: "ADOPTION_MAP, AUTONOMY_MAP, REVENUE_MAP"
      via: "enum-to-percentage+color lookup"
      pattern: "MAP.*pct.*label.*color"
---

<objective>
Replace the Phase 1 simple project list with rich project cards containing health bars, version history, conditional sections, and a summary stats bar.

Purpose: Transform raw project data into the visual dashboard Jeff sees on coaching calls -- every project's health, status, and roadmap at a glance.
Output: Updated index.html with card grid, health bars, summary stats, expandable version history, conditional Up Next and Priority sections.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-card-rendering-interactivity/02-RESEARCH.md

# Phase 1 built the HTML scaffold -- this plan modifies it
@.planning/phases/01-data-foundation-scaffold/01-02-SUMMARY.md

<interfaces>
<!-- From index.html (Phase 1 output) -- key patterns the executor must preserve/extend -->

CSS Custom Properties (in :root):
  --olive: #5C6B4F; --canyon: #8B6F5C; --gold: #C9953E; --cream: #FAF7F2; --charcoal: #2C2C2C;
  --font-heading: 'Source Serif 4'; --font-body: 'DM Sans';

Existing JS functions to PRESERVE:
  escapeHtml(str) -- sanitizes dynamic text for innerHTML
  validateProject(project, index) -- logs console warnings for malformed entries
  formatStatus(status) -- converts "in_progress" to "In Progress"
  renderFooterTimestamp(lastUpdated) -- sets footer text
  init() -- fetch pipeline, calls renderPage()

Data contract (window.__projects):
  project.name, project.slug, project.description, project.status
  project.currentVersion (string|null), project.deployedUrl (string|null), project.deployedDate (string|null)
  project.adoption, project.autonomy, project.revenueProximity (enum strings)
  project.versions (array of {version, date, notes}), project.upNext ({version, notes}|null)
  project.priority ({makesMoney, savesTime, servesClients, speedToFinish}|null)

Enum validation arrays (already defined):
  VALID_STATUSES, VALID_ADOPTION, VALID_AUTONOMY, VALID_REVENUE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS for cards, health bars, summary stats, and conditional sections</name>
  <files>index.html</files>
  <action>
In index.html's embedded style block, REPLACE the existing `.project-list` and `.project-item` styles with new styles for the card-based layout. KEEP all existing styles for body, header, footer, loading, error, status badges, and version-label.

Add these CSS sections:

1. **Summary stats bar** (.summary-stats): Flex row with wrapping, centered, gap between items. Each .stat-item has a colored left border (4px), white background, padding, small shadow. .stat-olive uses --olive border, .stat-gold uses --gold, .stat-canyon uses --canyon. .stat-count is large bold number, .stat-label is small text below.

2. **Project grid** (.project-grid): CSS grid, max-width 900px, single column, gap 1rem, margin 0 auto. Cards (.project-card) are white, rounded 8px, padding 1.5rem, subtle shadow. .project-card.hidden { display: none; } for future filtering.

3. **Card header** (.card-header): Flex row with wrapping. Project name is h3 with flex items (name text, status badge, version label). .card-meta below the h3 shows deployed date and link in smaller text. .deploy-link styled as --olive colored link.

4. **Health bars** (.health-bars): Section with 3 bars. Each .health-bar is a flex row: .health-label (7rem fixed width, 600 weight), .health-track (flex:1, 8px height, #e0ddd8 background, rounded), .health-fill (height 100%, rounded, transition width 0.3s), .health-text (7rem fixed, 0.8rem, right-aligned, #777).

5. **Version history** (.version-history details/summary): Summary styled with cursor pointer, font-weight 600, --olive color. .version-list has left border 2px --cream. Each .version-entry has padding-left, margin-bottom. version strong and .version-date styled.

6. **Up Next box** (.up-next): Gold left border (4px --gold), cream-tinted background (#fdf8f0), padding 1rem, rounded. .up-next-title is bold --gold colored.

7. **Priority scores** (.priority-scores): Flex row with wrapping, gap. Each .priority-item shows label and value (X/5). Small, compact display.

8. **Card section spacing**: .card-section class adds margin-top 1rem for each section within a card (health bars, version history, up next, priority).
  </action>
  <verify>
    <automated>cd /Users/joshuahogan/Projects/Tom\ Ferry\ Coaching && grep -c "health-bar\|summary-stats\|project-card\|up-next\|priority-scores\|version-history" index.html | xargs test 6 -le</automated>
  </verify>
  <done>All CSS classes for cards, health bars, summary stats, version history, up next, and priority scores exist in the style block</done>
</task>

<task type="auto">
  <name>Task 2: Add enum maps, computeStats, renderSummaryStats, and renderHealthBar helpers</name>
  <files>index.html</files>
  <action>
In index.html's embedded script block, add the enum-to-visual mapping constants and the summary stats / health bar helper functions. PRESERVE all existing functions (escapeHtml, validateProject, formatStatus, renderFooterTimestamp, init, renderPage). Do NOT modify renderPage yet -- that happens in Task 4.

**Add these constants** (after the existing VALID_* arrays):

```javascript
const ADOPTION_MAP = {
  none:          { pct: 0,   label: 'None',            color: 'var(--canyon)' },
  josh_only:     { pct: 33,  label: 'Josh Only',       color: 'var(--gold)' },
  team_uses:     { pct: 66,  label: 'Team Uses',       color: 'var(--gold)' },
  client_facing: { pct: 100, label: 'Client Facing',   color: 'var(--olive)' }
};

const AUTONOMY_MAP = {
  josh_dependent:    { pct: 33,  label: 'Josh Dependent',    color: 'var(--canyon)' },
  needs_guidance:    { pct: 66,  label: 'Needs Guidance',     color: 'var(--gold)' },
  fully_independent: { pct: 100, label: 'Fully Independent',  color: 'var(--olive)' }
};

const REVENUE_MAP = {
  internal_only:  { pct: 33,  label: 'Internal Only',  color: 'var(--canyon)' },
  client_touch:   { pct: 66,  label: 'Client Touch',   color: 'var(--gold)' },
  revenue_driver: { pct: 100, label: 'Revenue Driver',  color: 'var(--olive)' }
};
```

**Add these helper functions:**

1. `computeStats(projects)` -- Returns object with counts: shipped, inProgress, parkingLot, fullyIndependent, joshDependent, clientFacing. Uses .filter().length on the projects array.

2. `renderSummaryStats(projects)` -- Returns HTML string for the summary stats bar. 6 stat items:
   - Shipped (stat-olive), In Progress (stat-gold), Parking Lot (stat-canyon)
   - Fully Independent (stat-olive), Josh Dependent (stat-gold), Client Facing (stat-olive)
   All counts computed dynamically via computeStats().

3. `renderHealthBar(label, value, map)` -- Takes a label string ("Adoption"), a value string ("team_uses"), and the corresponding map (ADOPTION_MAP). Looks up pct/label/color from map. Returns HTML string for one .health-bar row. If value not found in map, render 0% width with "Unknown" label.

4. `renderHealthBars(project)` -- Returns HTML string with 3 health bars (inside a .health-bars.card-section div) for adoption, autonomy, revenueProximity using renderHealthBar().
  </action>
  <verify>
    <automated>cd /Users/joshuahogan/Projects/Tom\ Ferry\ Coaching && grep -q "ADOPTION_MAP" index.html && grep -q "AUTONOMY_MAP" index.html && grep -q "REVENUE_MAP" index.html && grep -q "computeStats" index.html && grep -q "renderSummaryStats" index.html && grep -q "renderHealthBar" index.html && grep -q "renderHealthBars" index.html && echo "MAPS_AND_STATS_PRESENT"</automated>
  </verify>
  <done>
    - Three enum maps (ADOPTION_MAP, AUTONOMY_MAP, REVENUE_MAP) define pct/label/color for each enum value
    - computeStats returns an object with 6 counts from the projects array
    - renderSummaryStats returns the HTML for the 6-item summary stats bar
    - renderHealthBar renders a single bar row with fill width, color, and label from enum map
    - renderHealthBars renders all 3 bars for a project
  </done>
</task>

<task type="auto">
  <name>Task 3: Add renderVersionHistory, renderUpNext, and renderPriorityScores functions</name>
  <files>index.html</files>
  <action>
In index.html's embedded script block, add three conditional section renderers. Place them after the renderHealthBars function added in Task 2. PRESERVE all existing functions.

1. `renderVersionHistory(project)` -- Returns HTML string. If project.versions is empty or not an array, return empty string. Otherwise return a `<details class="version-history card-section">` with `<summary>Version History (N)</summary>` and version entries sorted newest-first by date. Each entry shows bold version, date span, and notes paragraph. Use escapeHtml() on ALL dynamic values.

2. `renderUpNext(project)` -- Returns HTML string. If project.upNext is null/undefined or project.upNext.version is falsy, return empty string. Otherwise return a .up-next.card-section div with gold border showing "Up Next: vX.X" title and notes. Use escapeHtml() on all values.

3. `renderPriorityScores(project)` -- Returns HTML string. If project.status === 'shipped' OR project.priority is null/undefined, return empty string. Otherwise return a .priority-scores.card-section div with 4 items: Makes Money, Saves Time, Serves Clients, Speed to Finish, each showing value/5. Use escapeHtml() on values.

IMPORTANT: Every dynamic project field value inserted into innerHTML MUST be wrapped in escapeHtml(). This includes version strings, date strings, notes text, and score values.
  </action>
  <verify>
    <automated>cd /Users/joshuahogan/Projects/Tom\ Ferry\ Coaching && grep -q "renderVersionHistory" index.html && grep -q "renderUpNext" index.html && grep -q "renderPriorityScores" index.html && grep -q "escapeHtml" index.html && echo "CONDITIONAL_SECTIONS_PRESENT"</automated>
  </verify>
  <done>
    - renderVersionHistory returns expandable details/summary with entries sorted newest-first, or empty string when no versions
    - renderUpNext returns gold-bordered box when upNext exists, or empty string when null
    - renderPriorityScores returns score display for non-shipped projects with priority data, or empty string otherwise
    - All dynamic values escaped via escapeHtml()
  </done>
</task>

<task type="auto">
  <name>Task 4: Create renderCard and replace renderPage to assemble card grid</name>
  <files>index.html</files>
  <action>
In index.html's embedded script block, add renderCard() and REPLACE the existing renderPage() function. This wires together all functions from Tasks 2-3.

**Add renderCard(project):**

Returns HTML string for one .project-card div with data-status, data-slug, data-deployed, and data-autonomy attributes. Assembles:
- Card header: h3 with escapeHtml(name), status badge (using existing status-badge/status-{status} classes), version label (currentVersion or "unreleased")
- Card meta: deployed date (formatted nicely, or "Not yet deployed" if null) and deployed URL as clickable link (only if deployedUrl is truthy, otherwise omit link)
- Description paragraph with escapeHtml(description)
- renderHealthBars(project)
- renderVersionHistory(project)
- renderUpNext(project)
- renderPriorityScores(project)

Data attributes on the .project-card div:
- `data-status="${project.status}"`
- `data-slug="${project.slug}"`
- `data-deployed="${project.deployedDate || ''}"`
- `data-autonomy="${project.autonomy || ''}"`

**Replace renderPage(data):**

New renderPage should:
1. Get projects array from data.projects (default empty array)
2. Clear #app
3. Build HTML: renderSummaryStats(projects) + a .project-grid container with renderCard() for each project
4. Set innerHTML of #app
5. Store projects on window.__projects (keep existing behavior)

IMPORTANT: Every dynamic project field value inserted into innerHTML MUST be wrapped in escapeHtml(). This includes name, description, version, date, notes, URL text, and any other string from projects.json.
  </action>
  <verify>
    <automated>cd /Users/joshuahogan/Projects/Tom\ Ferry\ Coaching && grep -q "renderCard" index.html && grep -q "renderPage" index.html && grep -q "data-status" index.html && grep -q "data-slug" index.html && grep -q "data-deployed" index.html && grep -q "data-autonomy" index.html && grep -q "project-grid" index.html && echo "CARD_GRID_ASSEMBLED"</automated>
  </verify>
  <done>
    - renderCard produces a complete card div assembling header, meta, description, health bars, version history, up next, and priority scores
    - renderPage produces card grid with summary stats bar above it
    - Each card has data-status, data-slug, data-deployed, data-autonomy attributes for Plan 02 filtering/sorting
    - All dynamic values escaped via escapeHtml()
    - window.__projects still set for Plan 02 consumption
  </done>
</task>

</tasks>

<verification>
Open index.html in a browser:
1. Summary stats bar visible at top with 6 stat items showing correct counts (5 Shipped, 6 In Progress, 5 Parking Lot, etc.)
2. 16 project cards render in a grid below the stats
3. Each card has: name, colored status badge, version, deployed date or "Not yet deployed", clickable URL where available
4. Three health bars per card with color fills matching enum values
5. Click "Version History" on any card with versions -- expands to show entries newest-first
6. Cards with upNext data show gold-bordered "Up Next" box
7. Only non-shipped cards with priority data show priority scores section
8. No console errors
</verification>

<success_criteria>
All 16 projects render as rich cards with health bars, version history, conditional Up Next and Priority sections, and a summary stats bar with accurate live counts. Zero console errors. All dynamic content escaped.
</success_criteria>

<output>
After completion, create `.planning/phases/02-card-rendering-interactivity/02-01-SUMMARY.md`
</output>
